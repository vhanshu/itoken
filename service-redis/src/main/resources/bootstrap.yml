spring:
  cloud:
    config:
      uri: http://localhost:8777/
      name: common, service-redis
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: config